# receiptly：AI 协作说明

本文件为在此仓库工作的 AI 与开发者提供项目上下文和实现约束。开始任何功能前，先阅读 `README.md` 与 `requirements/requirements-specification.md`；后者是功能与验收的权威来源。

## 产品定义

receiptly 是单个家庭的私有购物小票账本：小票上传或手动录入 → AI/OCR 给出候选 → 用户逐行确认 → 已确认数据才进入账本、分析、价格历史和洞察。

当前正式名称为 **receiptly**。`requirements/project-brief.md` 和 `requirements/research.md` 中的 “Basket Ledger” 是同一产品的历史名称。

## 实现优先级

按需求文档的发布切片交付：

1. A：单家庭、手动收据、确认、分类、日期/类别支出、删除与基础测试。
2. B：私有原图上传、异步 OCR/AI 候选、确认页、失败回退、别名和总额/重复校验。
3. C：商品搜索、家庭跨店历史价、单位价、可比性、下钻、预算与确定性洞察。
4. D：成员邀请、CSV、审计、无障碍、移动体验、E2E、错误监测和隐私说明。

不要为了看似完整的产品而跳过 A 的可靠手动路径。

## 必须遵守的领域规则

- AI/OCR 的所有输出均为候选，必须可编辑，永远不能自动入账。
- 只有 `confirmed` 收据的有效行可以产生金额、`PriceObservation`、分析或洞察。
- `draft`、`processing`、`needs_review`、`deleted`、冲突或未确认内容不得作为统计输入。
- 修改已确认收据的金额、类别、商品合并或可比性时，必须回到 `needs_review`，重新确认后再反映到聚合数据。
- 总额与行项目不符时，阻止确认或要求显式调整项/理由；不得静默入账。
- 删除确认收据后，相关下钻、总额、价格和洞察都必须移除或重算。
- 同规格、可比较的家庭历史购买记录才能比较价格；展示日期、门店、规格、样本数和可比性边界。
- 所有业务数据必须受 `household_id` 约束；授权在服务端验证，不能信任客户端传入的家庭 ID。

## 明确禁止

- 不实现抓取、爬取、镜像、复制或公开再发布零售商站点、目录、图片、商品和促销数据。
- 不实现公开价格库、全市场/实时比价、原价判断、最便宜门店排名或自动跨规格/跨品牌等价结论。
- 不做医疗、营养、疾病、减重、儿童、孕期、过敏或药物建议，也不对商品打“健康/不健康”分。
- 不将小票、完整商品文本或家庭消费行为用于广告、公开展示或模型训练；analytics 不记录商品文本。

## UX、可靠性与无障碍

- 手动录入必须是与 OCR 同等完整的路径；AI、上传或网络失败时仍能完成收据。
- 处理上传/OCR 时显示进度、取消和重试；断网保留草稿并在恢复后继续。
- 覆盖空、加载、失败、无权限、邀请失效、模糊图片、AI 失败、总额不符、未分类、未确认、疑似重复、无筛选结果、样本不足、不可比、导出与删除二次确认等状态。
- 表单应有可见标签、错误摘要与焦点管理；表格可键盘使用；图表必须提供等价数据表，颜色不能是唯一信息通道；移动主要点击目标约为 44×44 CSS px。
- 长小票需支持分段保存和虚拟列表，不能丢失已编辑内容。

## 隐私与安全

- 小票图片与导出文件私有保存，只通过短期、受控链接访问。
- 在上传/确认界面提示裁切或遮蔽会员号和支付识别信息；不收集银行卡、病史、过敏、体重、孕期或用药资料。
- 日志不得记录原图、OCR 全文、令牌或支付信息；审计记录仅保存必要的操作者、动作、对象、时间和变更摘要。
- 外部 AI 服务必须记录供应商、用途和数据保留政策，并默认禁止训练用途。

## 开发与验证要求

- 金额以最小货币单位存储；处理退款、折扣、multi-buy 与优惠券时明确标记其可比性。
- 聚合应可从已确认的源数据重建；确认操作需原子化，避免“已入账但无行项目”或“价格更新但收据未确认”。
- 为金额、单位价、可比性、日期窗口和类别占比写单元测试；为授权、状态转换、确认原子性、删除重算写集成测试；关键路径写 E2E。
- 使用合成或明确授权且已脱敏的小票作为开发和测试夹具。

## 交付前自检

1. 未确认数据是否能以任何方式出现在统计、价格或洞察中？若能，停止并修复。
2. AI/OCR 和网络失败时，用户是否仍能完成手动入账？
3. 修改/删除确认收据是否让所有聚合和下钻同步更新？
4. 是否泄漏跨家庭数据、原图、商品文本或敏感识别信息？
5. 文案是否误称市场价格、健康结论或零售商数据？
6. 新界面是否包含键盘、焦点、错误、空态和移动端考虑？
